# Task

Add a small utility module `greet.js` at the repository root with a single
exported function:

```js
export function greet(name) {
  return `Hello, ${name}!`;
}
```

Keep it minimal — just the file with that one function. Do not add tests,
configuration, or unrelated changes.
