# Raw notes

- Shipped the incremental build engine spike; content-addressed cache works.
- Determinism policy still needs the stochastic sampling path.
- Next: wire the Anthropic provider so `md build` runs end to end.
