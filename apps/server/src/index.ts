/**
 * @makedown/server — [COMMERCIAL] cloud API.
 *
 * Public surface is filled in across Phase 2.2:
 *  - the workspace service + build orchestration (workspace.ts, builds.ts)
 *  - the Fastify HTTP/SSE API (api.ts)
 *  - the bootstrap entrypoint (main.ts)
 */
export const SERVER_PACKAGE_VERSION = "0.0.0";
