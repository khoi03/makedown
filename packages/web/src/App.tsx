/**
 * App shell — replaced with the real collaborative workspace UI in Phase 2.3
 * (editor + DAG + artifact viewer). This placeholder keeps the package building.
 */
export function App() {
  return (
    <main className="app-shell">
      <h1>Makedown</h1>
      <p>Collaborative build workspace — UI coming online.</p>
    </main>
  );
}
